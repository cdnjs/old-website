{{#each assets}}
    <tr class="version">
        <td class="versionlabel">{{version}}</td>
    </tr>
    {{#each files}}
        <tr class="library">
            <td>
                <input type="text" class="input-block-level" style="margin: 0" readonly value="//cdnjs.cloudflare.com/ajax/libs/{{../../name}}/{{../version}}/{{this}}">
            </td>
        </tr>
    {{/each}}
{{/each}}
